# ─── DATABASE ─────────────────────────────────────────────────────
# Get connection string from: Supabase Dashboard → Project Settings → Database
# Direct connection (requires IP allowlist):
#   postgresql://postgres:<password>@db.<ref>.supabase.co:5432/postgres
# Transaction pooler (no allowlist needed):
#   postgresql://postgres.<ref>:<password>@aws-1-eu-west-3.pooler.supabase.co:6543/postgres
# For async (SQLAlchemy): use postgresql+asyncpg:// prefix
DATABASE_URL=postgresql+asyncpg://postgres:mGQNUDG9W0CHkSdK@db.wkwgxhdtexjpqleuzyem.supabase.co:5432/postgres
# Add your current IP to Supabase → Project Settings → Database → Connection Pooling → Allowed IPs
# Or use the transaction pooler URL (no IP allowlist required)

# JWT Authentication
JWT_SECRET=change-this-in-production-use-openssl-rand-hex-32
JWT_ISSUER=sports-analysis-app
JWT_AUDIENCE=sports-analysis-app

# Admin Access Control (comma-separated emails)
ADMIN_EMAILS=ikambili34@gmail.com,ikambili34@live.com

# Local HDD Storage (GDPR: 90-day retention)
STORAGE_PATH=/mnt/data/videos
RETENTION_DAYS=90

# Payment Providers (managed via Doppler in production)
STRIPE_SECRET_KEY=
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=
MPESA_SHORTCODE=
MPESA_PASSKEY=
SERVER_BASE_URL=https://sports-analysis-app.onrender.com

# Server
HOST=0.0.0.0
PORT=8000
