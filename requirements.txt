# ============================================================
# Sports Analysis App — Python Dependencies
# ============================================================

# --- Core Computer Vision & Detection ---
ultralytics>=8.2.0
supervision>=0.21.0
opencv-python>=4.9.0
numpy>=1.24.0
torch>=2.0.0

# --- Multi-Object Tracking (included in supervision) ---
# ByteTrack is bundled with supervision

# --- Wearable Data Ingestion ---
fitdecode>=0.10.0          # Parse Garmin/ANT+ .FIT files
bleak>=0.21.0              # BLE device communication
openant>=1.3.0             # ANT+ protocol (HR straps, speed sensors)
# polar-python             # Polar device BLE (install from git if needed)
# garmin-ble               # Garmin direct BLE (install from git if needed)

# --- IMU & Biometric Processing ---
scikit-digital-health>=0.14.0   # IMU pipeline processing (SKDH)
floodlight>=0.4.0               # Sports analytics (tracking, metabolic power)
scipy>=1.11.0                   # Signal processing primitives

# --- Laser / LiDAR ---
open3d>=0.18.0             # 3D point cloud processing & visualization
laspy>=2.5.0               # Read/write LAS/LAZ LiDAR files
# pdal                     # Point cloud pipelines (install via conda if needed)
# rplidar                  # RPLiDAR hardware SDK (pip install rplidar-roboticia)

# --- ML & Classification ---
scikit-learn>=1.3.0        # K-Means, classifiers
pydantic>=2.0.0            # Data validation & models

# --- Config ---
pyyaml>=6.0.0

# --- Video/Audio Processing ---
moviepy>=1.0.0             # Video editing & highlight assembly
librosa>=0.10.0            # Audio analysis (crowd noise detection)

# --- API (Phase 3) ---
fastapi>=0.110.0
uvicorn>=0.29.0
websockets>=12.0
python-dotenv>=1.0.0

# --- Auth (Phase 5) ---
python-jose[cryptography]>=3.3
bcrypt>=4.1
google-auth>=2.29
python-multipart>=0.0

# --- Payments (Phase 5) ---
stripe>=10.4
requests>=2.31

# --- Dev Tools ---
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
ruff>=0.1.0
black>=23.0.0
mypy>=1.5.0
